<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
    <!-- Required for Mac App Store submission -->
    <key>com.apple.security.app-sandbox</key>
    <true/>

    <!-- Microphone access (recording) -->
    <key>com.apple.security.device.audio-input</key>
    <true/>

    <!-- Network: outbound (Google Calendar API, model downloads) -->
    <key>com.apple.security.network.client</key>
    <true/>

    <!-- User-selected file access (storage folder picker, export) -->
    <key>com.apple.security.files.user-selected.read-write</key>
    <true/>

    <!-- Downloads folder (export bundles) -->
    <key>com.apple.security.files.downloads.read-write</key>
    <true/>
</dict>
</plist>
